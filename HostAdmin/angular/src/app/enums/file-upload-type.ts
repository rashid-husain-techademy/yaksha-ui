export enum FileUploadType {
    User = 0,
    Course = 10,
    SubjectiveQuestion = 20,
    HackathonScheduleLogo = 30,
    TenantLogo = 40
}

export enum UploadType {
    FileUpload = 1,
    ImageUpload = 2
}

export enum FileUploadMedium {
    System = 1,
    Mobile = 2
}